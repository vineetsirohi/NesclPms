@model NesclPms.WebUI.Models.ChangeLabelViewModel
@{
    ViewBag.Title = "Change Name";
}

<h2>@ViewBag.Title.</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <label>Name</label>
        @Html.TextBoxFor(x => x.Label, new { @class = "form-control" })
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    @*@Html.ActionLink("Manage", "ChangeLabel", null, new { @class = "btn btn-default" })*@
}

@*@using (Html.BeginForm("ChangeLabel", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <h4>Change Name</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Label, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Label, new { @class = "form-control" })
        </div>
    </div>
    
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Change name" class="btn btn-default" />
        </div>
    </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}*@